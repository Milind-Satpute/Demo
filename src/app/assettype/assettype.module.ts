import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { NetappComponent } from './netapp/netapp.component';

@NgModule({
  imports: [
    CommonModule
  ],
  declarations: [NetappComponent]
})
export class AssettypeModule { }
